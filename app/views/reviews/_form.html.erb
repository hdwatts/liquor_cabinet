<%= form_for [@recipe, @new_review], remote: true do |f| %>
  <% if signed_in? %>
  <%= f.text_area :comment, rows: '3', placeholder: "Comment here..." %>
  <% end %>
  <%= f.submit 'Leave Comment'%>
<% end %>
