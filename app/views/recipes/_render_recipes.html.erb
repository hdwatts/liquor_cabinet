<% if @recipes && !@recipes.empty? %>
  <h2 class="search-msg"><%= @search_msg %></h2>
  <% @recipes.each do |recipe| %>
    <%= render partial: "recipes/recipe_teaser", locals: {the_recipe: recipe} %>
  <% end %>  
<% else %>
  <h1>No Recipes Found</h1>
<% end %>