<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
     <%= link_to 'Cocktail Cabinet', root_path, class: 'navbar-brand' %>
    <% if current_user %>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
         <a href="#" class="dropdown-toggle parent" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
          <div class="profile-img" style="background-image:url('<%= current_user.img_url %>')"><span></span></div>
          <%= current_user.first_name %><span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to "My Recipes", user_path(current_user) %></li>
          <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
          <li role="separator" class="divider"></li>
          <li><a href= '/logout' class="navbar-btn">Sign out</a></li>
        </ul>
        </li>
        <li class="add-new"><a href= '/recipes/new' class="parent">Add Recipe</a></li>
      </ul>
      <% else %>
      <div class="navbar-right">
      <a href= '/login' type="button" class="btn btn-default navbar-btn">Sign in</a>
      <a href= '/users/new' type="button" class="btn btn-default navbar-btn">Sign Up</a>
    </div>
    <% end  %>
  </div>
</nav>
